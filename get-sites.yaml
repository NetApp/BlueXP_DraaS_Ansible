---
- name: Make HTTP GET request to fetch sites
  hosts: localhost
  gather_facts: no
  vars_files:
    - vars/env/extravars

  tasks:
    - name: Fetch sites
      uri:
        url: "https://snapcenter.cloudmanager.cloud.netapp.com/api/vmdr/sites"
        method: get
        return_content: yes
        headers:
          Content-Type: "application/json"  
          x-agent-id: "{{ agentid }}" 
          Authorization: "{{ authorization }}"
          x-account-id: "{{ accountid }}"
      register: result


    - name: Print the fetched sites
      debug:
        var: result.json


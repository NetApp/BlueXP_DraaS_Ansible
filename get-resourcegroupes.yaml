---
- name: Make HTTP GET request to fetch resourcegroups
  hosts: localhost
  gather_facts: no
  vars_files:
    - vars/env/extravars

  tasks:
    - name: Fetch resourcegroups
      uri:
        url: "https://snapcenter.cloudmanager.cloud.netapp.com/api/vmdr/resourcegroups?"
        method: get
        return_content: yes
        headers:
          Content-Type: "application/json"  
          x-agent-id: "{{ agentid }}" 
          Authorization: "{{ authorization }}"
          x-account-id: "{{ accountid }}"
      register: result

    - name: Print the fetched resourcegroups
      debug:
        var: result.json

